---
phase: 05-workflow-import-and-build-validation
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - cap-n8n-plugin/bin/cap-n8n.js
  - cap-n8n-plugin/lib/workflows/validate-command.js
  - cap-n8n-plugin/index.js
  - package.json
  - test/integration/n8n-workflow-phase5.test.js
  - README.md
  - docs/manual-visual-showcase.md
autonomous: true
requirements: [IMPORT-01, IMPORT-02, IMPORT-03, IMPORT-04, IMPORT-05, IMPORT-06, IMPORT-07, VERIFY-03]
user_setup: []
must_haves:
  truths:
    - "D-20: cap-n8n validate --app demo-app runs the same validation engine as CAP build and returns non-zero only for error diagnostics."
    - "VERIFY-03: npm test and focused Phase 5 integration tests cover local import, live import, generated CDS, sanitized artifacts, direct CLI validation, and CAP build validation."
    - "D-11/D-15: CLI help and npm wrappers support import and validate without literal API-key flags or secret output."
    - "IMPORT-01 through IMPORT-07: Phase 5 behavior is verified through package-owned modules and demo-app artifacts, not demo-only glue."
    - "Documentation updates keep README.md and docs/manual-visual-showcase.md aligned with any Phase 5 import, validate, artifact layout, or demo-flow behavior changes."
  artifacts:
    - path: cap-n8n-plugin/bin/cap-n8n.js
      provides: "Import and validate package CLI"
      contains: "validate"
    - path: cap-n8n-plugin/lib/workflows/validate-command.js
      provides: "CLI adapter for shared validator"
      exports: [runValidateCommand]
    - path: test/integration/n8n-workflow-phase5.test.js
      provides: "Aggregate Phase 5 integration evidence"
      contains: "cap-n8n validate"
    - path: README.md
      provides: "Phase 5 developer command and testing documentation"
      contains: "n8n:workflow:validate"
    - path: docs/manual-visual-showcase.md
      provides: "Manual visual showcase steps for implemented Phase 5 functionality"
      contains: "cap-n8n validate"
  key_links:
    - from: cap-n8n-plugin/bin/cap-n8n.js
      to: cap-n8n-plugin/lib/workflows/validate-command.js
      via: "validate subcommand dispatch"
      pattern: "runValidateCommand"
    - from: cap-n8n-plugin/lib/workflows/validate-command.js
      to: cap-n8n-plugin/lib/workflows/validate.js
      via: "validateWorkflowAnnotations"
      pattern: "validateWorkflowAnnotations"
    - from: package.json
      to: cap-n8n-plugin/bin/cap-n8n.js
      via: "npm script wrappers"
      pattern: "n8n:workflow:validate"
---

<objective>
Wire direct CLI validation and run aggregate Phase 5 verification.

Purpose: CAP developers need both cds build validation and a focused cap-n8n validate command for debugging, tests, and manual showcase.
Output: cap-n8n validate command, npm wrapper, package exports, aggregate integration test, npm test evidence, build/compile validation evidence, and secret/sanitization source gates.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-workflow-import-and-build-validation/05-CONTEXT.md
@.planning/phases/05-workflow-import-and-build-validation/05-RESEARCH.md
@.planning/phases/05-workflow-import-and-build-validation/05-PATTERNS.md
@.planning/phases/05-workflow-import-and-build-validation/05-02-SUMMARY.md
@.planning/phases/05-workflow-import-and-build-validation/05-03-SUMMARY.md
@cap-n8n-plugin/bin/cap-n8n.js
@cap-n8n-plugin/lib/workflows/validate.js
@cap-n8n-plugin/lib/workflows/BuildValidationPlugin.js
@demo-app/n8n/manifest.json
@demo-app/srv/admin-service.cds
@package.json
@README.md
@docs/manual-visual-showcase.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire cap-n8n validate to the shared validator</name>
  <read_first>
    @cap-n8n-plugin/bin/cap-n8n.js
    @cap-n8n-plugin/lib/workflows/validate.js
    @cap-n8n-plugin/lib/workflows/diagnostics.js
    @cap-n8n-plugin/package.json
    @package.json
    @.planning/phases/05-workflow-import-and-build-validation/05-CONTEXT.md
  </read_first>
  <files>cap-n8n-plugin/bin/cap-n8n.js, cap-n8n-plugin/lib/workflows/validate-command.js, cap-n8n-plugin/index.js, package.json</files>
  <behavior>
    - cap-n8n validate --app demo-app loads the demo CAP model and demo-app/n8n artifacts, reports warnings/errors from validateWorkflowAnnotations, and exits 0 when only warnings or no diagnostics exist.
    - cap-n8n validate --app &lt;error-fixture&gt; exits 1 when validator errors exist and prints sanitized diagnostics.
    - cap-n8n validate --app demo-app --json emits machine-readable sanitized diagnostics with errors, warnings, and diagnostics arrays.
    - npm run n8n:workflow:validate -- --app demo-app invokes the same command per D-11 and D-20.
  </behavior>
  <action>Add validate-command.js as a CLI adapter that resolves --app, loads the CAP model with generated n8n artifacts included, invokes validateWorkflowAnnotations, formats deterministic text diagnostics, supports --json, and sets exit code 1 only when errors exist. Extend cap-n8n-plugin/bin/cap-n8n.js with the validate subcommand. Export validateWorkflowAnnotations and workflowTools from cap-n8n-plugin/index.js if not already exported. Add root script "n8n:workflow:validate": "cap-n8n validate" without removing existing scripts. CLI help must list import and validate and must not list --api-key or any literal secret option per D-15.</action>
  <verify>
    <automated>node cap-n8n-plugin/bin/cap-n8n.js validate --app demo-app</automated>
    <automated>node cap-n8n-plugin/bin/cap-n8n.js validate --app demo-app --json</automated>
    <automated>npm run n8n:workflow:validate -- --app demo-app</automated>
  </verify>
  <acceptance_criteria>
    - The validate command reads the same generated artifact manifests and CSN annotation data as BuildValidationPlugin.
    - Text diagnostics identify entity, annotation, workflow key/reference, input name, severity, code, and reason.
    - JSON diagnostics are parseable and contain no raw secrets, auth headers, request bodies, workflow payloads, stack traces, or .env values.
    - CLI help contains no literal --api-key flag.
  </acceptance_criteria>
  <done>Direct package CLI validation is available and uses the same sanitized validator as CAP build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add aggregate Phase 5 integration and source gates</name>
  <read_first>
    @test/integration/n8n-workflow-artifacts.test.js
    @test/integration/n8n-workflow-import.test.js
    @test/integration/n8n-workflow-live-import.test.js
    @test/integration/n8n-workflow-build-validation.test.js
    @cap-n8n-plugin/bin/cap-n8n.js
    @demo-app/n8n/manifest.json
    @demo-app/srv/admin-service.cds
    @package.json
  </read_first>
  <files>test/integration/n8n-workflow-phase5.test.js</files>
  <behavior>
    - Aggregate tests run cap-n8n import against a temp app from local fixture data, then cap-n8n validate against that app, then cds compile/build against that app.
    - Aggregate tests run cap-n8n validate --app demo-app and confirm existing AdminService.Books annotation resolves through manifest acceptedReferences.
    - Aggregate tests assert demo-app/n8n generated artifacts and CLI output contain no leaked secret values, personal metadata values, unsafe workflow fields, pinned/static payloads, or runtime-only values.
    - Aggregate tests assert npm test includes the Phase 5 integration files through the existing test:integration script.
  </behavior>
  <action>Create an aggregate Vitest integration suite that proves the full Phase 5 chain: local import writes artifacts, generated CDS compiles, direct CLI validation passes, CAP build validation runs, warning-only fixtures pass, error fixtures fail, and demo-app annotations validate against cap-test-trigger accepted references. Limit implementation source gates under cap-n8n-plugin/lib/workflows and cap-n8n-plugin/bin to literal sample secret values, unsafe .env file reads, dotenv-style .env ingestion, or logging of concrete auth header values; do not fail on legitimate credential-related identifiers such as apiKey, authorization, credentials, headers, or X-N8N-API-KEY in live-import code. Use structured JSON scans for demo-app/n8n and generated CLI output to forbid leaked sensitive values and unsafe workflow fields while allowing manifest redaction path names. Keep tests offline with temp apps and fake HTTP where needed.</action>
  <verify>
    <automated>npm run test:integration -- --run test/integration/n8n-workflow-artifacts.test.js test/integration/n8n-workflow-import.test.js test/integration/n8n-workflow-live-import.test.js test/integration/n8n-workflow-build-validation.test.js test/integration/n8n-workflow-phase5.test.js</automated>
    <automated>npx cds compile demo-app/db demo-app/srv demo-app/app demo-app/n8n --to csn</automated>
    <automated>node cap-n8n-plugin/bin/cap-n8n.js validate --app demo-app</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 5 integration command passes and covers IMPORT-01 through IMPORT-07 plus VERIFY-03.
    - npx cds compile includes demo-app/n8n and succeeds.
    - cap-n8n validate --app demo-app exits 0 and resolves webhook-test/cap-test-trigger to cap-test-trigger.
    - npm test passes with smoke plus integration coverage.
    - Implementation source gates fail only for literal sample secret values, unsafe `.env` reads/ingestion, or logged concrete auth header values, not for valid credential-related identifiers required by live import.
    - Generated artifact and CLI-output gates parse structured JSON/text and fail on leaked secret values, personal metadata values, unsafe workflow.json fields, pinned/static payload values, request/response body values, stack traces, or `.env` contents while allowing manifest redaction path names.
  </acceptance_criteria>
  <done>Phase 5 has aggregate integration evidence for import, typed artifacts, direct validation, CAP build validation, and secret-safe outputs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update developer docs and manual visual showcase</name>
  <read_first>
    @README.md
    @docs/manual-visual-showcase.md
    @package.json
    @demo-app/n8n/manifest.json
    @.planning/phases/05-workflow-import-and-build-validation/05-CONTEXT.md
    @.planning/phases/05-workflow-import-and-build-validation/05-01-SUMMARY.md
    @.planning/phases/05-workflow-import-and-build-validation/05-02-SUMMARY.md
    @.planning/phases/05-workflow-import-and-build-validation/05-03-SUMMARY.md
  </read_first>
  <files>README.md, docs/manual-visual-showcase.md</files>
  <behavior>
    - README.md documents the current Phase 5 package CLI and npm wrapper commands for local workflow import, live workflow import, and direct validation.
    - README.md documents the app-root n8n artifact layout and which sanitized workflow JSON, sidecar schema, manifest, and generated CDS files are committed.
    - docs/manual-visual-showcase.md explains how a reviewer with no codebase knowledge can visually demonstrate the implemented Phase 5 behavior, including expected command output and where generated artifacts appear.
    - If any Phase 5 implementation changes command names, npm script names, artifact paths, generated CDS names, or demo validation flow, both docs are updated in the same plan before final verification.
  </behavior>
  <action>Update README.md and docs/manual-visual-showcase.md to match the final Phase 5 implementation. Include exact commands for package CLI import/validate and npm wrappers, expected success/failure output shape, app-root demo-app/n8n artifact paths, sanitized artifact guarantees, and the manual showcase sequence for validating existing AdminService.Books annotations against generated workflow artifacts. Keep caveats honest if live n8n import, --all behavior, CAP build plugin integration, or generated CDS behavior differs from the planned command shape. Do not include API keys, private URLs, personal metadata, or copied secret values.</action>
  <verify>
    <automated>rg -n "n8n:workflow:validate|cap-n8n validate|demo-app/n8n|manual visual showcase" README.md docs/manual-visual-showcase.md</automated>
    <automated>rg -n "ghp_|gho_|github_pat_|sk-[A-Za-z0-9]|BEGIN .*PRIVATE KEY|N8N_API_KEY=.*[A-Za-z0-9]" README.md docs/manual-visual-showcase.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains exact Phase 5 command names for local import, live import, and validate, or explicitly states any Phase 5 command that remains unavailable.
    - README.md names the committed artifact layout under demo-app/n8n and distinguishes sanitized workflow JSON, sidecar schema, manifest, and generated CDS.
    - docs/manual-visual-showcase.md includes a reviewer-oriented Phase 5 section with setup, commands to run, expected output, where to look in the filesystem, and what the n8n/CAP UI or terminal proves.
    - Both docs avoid committed secrets, personal metadata, private URLs, raw API keys, and misleading claims about unimplemented functionality.
  </acceptance_criteria>
  <done>README.md and the manual visual showcase guide reflect the final Phase 5 import, validation, artifact, and demo behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI validate output -> developer terminal | Diagnostics may expose app metadata or secrets if not sanitized. |
| app-root n8n artifacts -> CAP build/direct validator | Generated files influence build and CLI exit behavior. |
| aggregate tests -> repository state | Verification scans generated and source files for accidental secret/personal metadata. |
| implementation docs -> reviewers/developers | README and showcase docs can mislead users or leak example secrets if not updated with final behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-I | Information Disclosure | cap-n8n-plugin/lib/workflows/validate-command.js | mitigate-blocking | Reuse sanitized diagnostics, support JSON output without raw workflow payloads, limit implementation source gates to literal secrets and unsafe .env reads, and scan generated artifacts/CLI output for leaked sensitive values. |
| T-05-04-T | Tampering | cap-n8n-plugin/bin/cap-n8n.js | mitigate | Dispatch only known subcommands import and validate, reject unknown flags, and preserve Plan 05-02 path controls. |
| T-05-04-R | Repudiation | test/integration/n8n-workflow-phase5.test.js | mitigate | Aggregate tests assert diagnostics identify entity, workflow reference, input, code, severity, and reason. |
| T-05-04-D | Denial of Service | npm test and CAP build validation | mitigate | Keep automated tests offline with temp apps/fake HTTP and no Docker dependency; validation scans bounded manifests. |
| T-05-SC | Tampering | npm installs | mitigate | No new package installs are planned; aggregate tests use existing Vitest, CAP, Node, and npm workspace tooling. |
| T-05-04-DOC | Information Disclosure | README.md and docs/manual-visual-showcase.md | mitigate-blocking | Update docs from final implementation, verify documented commands match package scripts, and scan docs for token/private-key/API-key patterns before completion. |
</threat_model>

<verification>
Run final Phase 5 verification:
- `npm run test:integration -- --run test/integration/n8n-workflow-artifacts.test.js test/integration/n8n-workflow-import.test.js test/integration/n8n-workflow-live-import.test.js test/integration/n8n-workflow-build-validation.test.js test/integration/n8n-workflow-phase5.test.js`
- `npx cds compile demo-app/db demo-app/srv demo-app/app demo-app/n8n --to csn`
- `node cap-n8n-plugin/bin/cap-n8n.js validate --app demo-app`
- `npm test`
- `rg -n "n8n:workflow:validate|cap-n8n validate|demo-app/n8n|manual visual showcase" README.md docs/manual-visual-showcase.md`
- Secret/sanitization gates over generated `demo-app/n8n` artifacts, CLI help, CLI diagnostic output, and implementation sources, with implementation-source checks limited to literal sample secrets and unsafe `.env` access.
- Secret scan over README.md and docs/manual-visual-showcase.md for GitHub tokens, private keys, OpenAI-style keys, literal API keys, and personal production metadata.
</verification>

<success_criteria>
Plan 05-04 is complete when direct CLI validation works, aggregate Phase 5 tests pass, CAP compile/build validation behavior is proven, npm test passes, source gates confirm generated artifacts and diagnostics avoid secrets and personal metadata, and README.md plus docs/manual-visual-showcase.md accurately describe the final Phase 5 commands, artifact layout, expected outputs, and manual showcase flow.
</success_criteria>

<output>
Create `.planning/phases/05-workflow-import-and-build-validation/05-04-SUMMARY.md` when done.
</output>
