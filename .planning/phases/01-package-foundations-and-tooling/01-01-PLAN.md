---
phase: 01-package-foundations-and-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cap-n8n-plugin/package.json
  - cap-n8n-plugin/index.js
  - demo-app/package.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "FOUND-01: A developer can require cap-n8n-plugin through the package name and receive a public N8nWorkflowService export."
    - "FOUND-02: cap-n8n-plugin metadata declares Node, CAP peer, package files, package exports, description, keywords, and license expectations."
    - "The demo app retains its existing Book-created n8n proof while avoiding an internal implementation path where the public package boundary supports it."
  artifacts:
    - path: "cap-n8n-plugin/package.json"
      provides: "CAP plugin package metadata and public export map"
      contains: "peerDependencies"
    - path: "cap-n8n-plugin/index.js"
      provides: "CommonJS package-level export for N8nWorkflowService"
      exports: ["N8nWorkflowService"]
    - path: "demo-app/package.json"
      provides: "Demo app CAP n8n binding using package-owned plugin dependency"
      contains: "cap-n8n-plugin"
  key_links:
    - from: "cap-n8n-plugin/index.js"
      to: "cap-n8n-plugin/lib/N8nWorkflowService.js"
      via: "CommonJS require"
      pattern: "require\\('./lib/N8nWorkflowService\\.js'\\)"
    - from: "demo-app/package.json"
      to: "cap-n8n-plugin"
      via: "workspace package dependency"
      pattern: "\"cap-n8n-plugin\""
---

<objective>
Repair the CAP plugin package boundary so consuming code can load the plugin through `cap-n8n-plugin` instead of internal file paths.

Purpose: This establishes the package-owned CAP surface required by FOUND-01 and FOUND-02, implementing D-09, D-10, and the package-boundary part of D-16 while honoring D-03 through D-08 as contextual GitHub issue constraints.
Output: Updated CAP plugin package metadata, a public CommonJS export, and demo app binding cleanup where safe.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-package-foundations-and-tooling/01-CONTEXT.md
@.planning/phases/01-package-foundations-and-tooling/01-RESEARCH.md
@.planning/phases/01-package-foundations-and-tooling/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@cap-n8n-plugin/package.json
@cap-n8n-plugin/index.js
@cap-n8n-plugin/cds-plugin.js
@cap-n8n-plugin/lib/N8nWorkflowService.js
@demo-app/package.json
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL | Developers can consume both packages through real boundaries and repeatable local tooling | Plans 01-04 cover package boundaries, n8n package loadability, local tooling, pinned infrastructure, and smoke verification. |
| REQ | FOUND-01 | Plan 01 covers package-level CAP plugin consumption. |
| REQ | FOUND-02 | Plan 01 covers CAP plugin metadata; Plan 04 refreshes lockfiles after all metadata changes. |
| REQ | FOUND-03 | Plan 03 covers repo-local CAP, n8n, build, and script tooling. |
| REQ | FOUND-04 | Plan 04 covers package loadability smoke verification. |
| REQ | FOUND-05 | Plan 03 covers pinned n8n local infrastructure. |
| REQ | NODE-01 | Plan 02 covers n8n community-node package loadability. |
| RESEARCH | Use npm workspaces, local @sap/cds-dk, @n8n/node-cli, pinned n8n, and smoke checks | Plans 02-04 cover these without adding runtime workflow behavior. |
| RESEARCH | Vitest slopcheck SUS warning | Plan 04 includes a blocking human verification checkpoint before adding Vitest. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08 | Plans use Koerbser-assigned GitHub issues only as context; no Phase 1 plan claims issue closure. |
| CONTEXT | D-09, D-10, D-16 | Plan 01 implements public CAP package export, metadata, and demo binding preservation. |
| CONTEXT | D-11, D-12, D-13, D-15 | Plans 03-04 implement repo-local commands, non-failing package test scripts, pinned n8n image, and smoke tests. |
| CONTEXT | D-14 | Plan 02 implements the n8n node package loadability baseline. |
| CONTEXT | D-17, D-18, D-19, D-20, D-21, D-22 | Every plan action excludes runtime start semantics, cancel/query, mock/profiles/retry, annotations, import/build validation, and BTP/hybrid docs. |
| CONTEXT | D-23, D-24, D-25 | This plan set uses four waves/plan boundaries, conventional file names, and package-boundary demo cleanup at the agent's discretion. |

No source items are unplanned. Deferred ideas from CONTEXT.md are excluded.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish the CAP service class through the package entry</name>
  <read_first>
    - cap-n8n-plugin/index.js
    - cap-n8n-plugin/lib/N8nWorkflowService.js
    - .planning/phases/01-package-foundations-and-tooling/01-PATTERNS.md
  </read_first>
  <files>cap-n8n-plugin/index.js</files>
  <behavior>
    - Requiring `cap-n8n-plugin` returns an object with `N8nWorkflowService`.
    - `N8nWorkflowService` is the same class exported by `cap-n8n-plugin/lib/N8nWorkflowService.js`.
    - The entry file stays CommonJS and does not add start/cancel/query/mock/profile/retry/annotation/import behavior per D-17 through D-22.
  </behavior>
  <action>Implement D-09 by exporting `N8nWorkflowService` from `cap-n8n-plugin/index.js` using CommonJS. Keep the file focused on public package exports only; do not modify `cap-n8n-plugin/lib/N8nWorkflowService.js` unless a loadability issue requires a narrow import fix.</action>
  <verify>
    <automated>node -e "const plugin=require('./cap-n8n-plugin'); const service=require('./cap-n8n-plugin/lib/N8nWorkflowService.js'); if (plugin.N8nWorkflowService !== service) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `require('./cap-n8n-plugin').N8nWorkflowService` is a function.
    - `cap-n8n-plugin/index.js` has no empty export surface.
    - No deferred runtime behavior from D-17 through D-22 is introduced.
  </acceptance_criteria>
  <done>CAP plugin consumers can load `N8nWorkflowService` through the package entry point.</done>
</task>

<task type="auto">
  <name>Task 2: Align CAP plugin package metadata</name>
  <read_first>
    - cap-n8n-plugin/package.json
    - .planning/phases/01-package-foundations-and-tooling/01-RESEARCH.md
    - .planning/codebase/CONCERNS.md
  </read_first>
  <files>cap-n8n-plugin/package.json</files>
  <action>Implement D-10 by updating `cap-n8n-plugin/package.json` with `description`, `keywords`, `engines.node` set to `>=20`, `peerDependencies.@sap/cds` set to `>=9 <10`, `files` allowlisting `index.js`, `cds-plugin.js`, and `lib/`, `main: "index.js"`, and `exports` for `"."` plus a public service subpath such as `"./service": "./lib/N8nWorkflowService.js"` if needed for CAP `impl` binding. Keep license `ISC` unless an existing manifest already proves a different project-wide decision.</action>
  <verify>
    <automated>node -e "const p=require('./cap-n8n-plugin/package.json'); if(!p.exports['.']||!p.peerDependencies['@sap/cds']||!p.engines.node||!p.files.includes('cds-plugin.js')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Package metadata declares the CAP peer dependency and Node engine.
    - Package files are allowlisted for publishable package contents.
    - Package exports make the public entry explicit.
  </acceptance_criteria>
  <done>`cap-n8n-plugin/package.json` accurately describes the runtime host requirements and public package surface for FOUND-02.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve demo consumption through the package boundary</name>
  <read_first>
    - demo-app/package.json
    - cap-n8n-plugin/package.json
    - cap-n8n-plugin/cds-plugin.js
  </read_first>
  <files>demo-app/package.json</files>
  <action>Implement D-16 by keeping the demo app's `cap-n8n-plugin` workspace dependency and replacing `cds.requires.n8n.impl` only if the package export map from Task 2 supports a package-level service target such as `cap-n8n-plugin/service`. Preserve `cds.requires.n8n.credentials.baseUrl`, `apiKey`, and the CAP server port. If CAP cannot resolve the package subpath reliably, leave the existing implementation path and document that Plan 04 smoke coverage proves package-level consumption separately per D-25.</action>
  <verify>
    <automated>node -e "const p=require('./demo-app/package.json'); if(!p.dependencies['cap-n8n-plugin']||!p.cds.requires.n8n.impl) process.exit(1); if(!p.cds.requires.n8n.credentials.baseUrl) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Demo app still depends on `cap-n8n-plugin`.
    - Existing n8n base URL and env-backed API key configuration remain intact.
    - Demo app does not lose the Book-created proof path.
  </acceptance_criteria>
  <done>The demo app remains a working consumer proof while the reusable package boundary is repaired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app -> cap-n8n-plugin package | Host CAP apps import package code and rely on package metadata to select compatible runtime versions. |
| package manifest -> npm pack/install | Package metadata controls what files and peer requirements are exposed to consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | cap-n8n-plugin/package.json | mitigate | Use explicit `exports`, `files`, `engines`, and CAP peer metadata so consumers do not depend on internal files or incompatible runtime versions. |
| T-01-02 | Information Disclosure | package `files` allowlist | mitigate | Allowlist package files to avoid publishing local-only or secret-bearing artifacts. |
| T-01-SC | Tampering | npm package installs | transfer | No package install occurs in this plan; Plan 04 owns lockfile refresh and the Vitest slopcheck checkpoint. |
</threat_model>

<verification>
Run the task-level automated checks. After Plan 04 completes, the root smoke command must also prove this package boundary through the workspace package name.
</verification>

<success_criteria>
FOUND-01 and FOUND-02 are satisfied for the CAP plugin side when `require('cap-n8n-plugin')` exposes `N8nWorkflowService`, package metadata declares CAP/Node constraints, and the demo app still has a valid n8n service binding.
</success_criteria>

<output>
Create `.planning/phases/01-package-foundations-and-tooling/01-01-SUMMARY.md` when done.
</output>
