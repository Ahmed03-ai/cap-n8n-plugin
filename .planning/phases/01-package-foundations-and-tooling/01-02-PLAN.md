---
phase: 01-package-foundations-and-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cap-n8n-node/package.json
  - cap-n8n-node/index.js
  - cap-n8n-node/nodes/SapCap/SapCap.node.ts
  - cap-n8n-node/credentials/SapCapApi.credentials.ts
  - cap-n8n-node/tsconfig.json
autonomous: true
requirements:
  - NODE-01
must_haves:
  truths:
    - "NODE-01: n8n can discover a community-node package shape for SAP CAP from cap-n8n-node metadata."
    - "The package has declared n8n node and credential manifest paths."
    - "The node package provides loadable source files without implementing CAP OData operations in Phase 1."
  artifacts:
    - path: "cap-n8n-node/package.json"
      provides: "n8n community-node metadata and scripts"
      contains: "n8n-community-node-package"
    - path: "cap-n8n-node/nodes/SapCap/SapCap.node.ts"
      provides: "SAP CAP node class for n8n package loadability"
      contains: "SapCap"
    - path: "cap-n8n-node/credentials/SapCapApi.credentials.ts"
      provides: "SAP CAP credential class for n8n package loadability"
      contains: "SapCapApi"
  key_links:
    - from: "cap-n8n-node/package.json"
      to: "cap-n8n-node/nodes/SapCap/SapCap.node.ts"
      via: "n8n.nodes manifest path"
      pattern: "SapCap\\.node\\.js"
    - from: "cap-n8n-node/package.json"
      to: "cap-n8n-node/credentials/SapCapApi.credentials.ts"
      via: "n8n.credentials manifest path"
      pattern: "SapCapApi\\.credentials\\.js"
---

<objective>
Create the n8n community-node package loadability baseline for SAP CAP.

Purpose: This satisfies NODE-01 and implements D-14 without starting the rich n8n operation work reserved outside Phase 1 by D-17 through D-22.
Output: n8n-compatible package metadata, node source file, credential source file, and package-local TypeScript config.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-package-foundations-and-tooling/01-CONTEXT.md
@.planning/phases/01-package-foundations-and-tooling/01-RESEARCH.md
@.planning/phases/01-package-foundations-and-tooling/01-PATTERNS.md
@.planning/codebase/STACK.md
@cap-n8n-node/package.json
@cap-n8n-node/index.js
@mockups/n8n-node-mockup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert cap-n8n-node metadata to n8n community-node shape</name>
  <read_first>
    - cap-n8n-node/package.json
    - .planning/phases/01-package-foundations-and-tooling/01-RESEARCH.md
    - .planning/phases/01-package-foundations-and-tooling/01-PATTERNS.md
  </read_first>
  <files>cap-n8n-node/package.json</files>
  <action>Implement D-14 and D-24 by keeping the workspace folder `cap-n8n-node/` and setting package metadata to a n8n-compatible package name, defaulting to `n8n-nodes-sap-cap` unless an existing source file proves a different accepted name. Add `description`, `main`, `types`, `scripts.build`, `scripts.lint`, `scripts.dev`, and `scripts.test` using `n8n-node`, add `keywords` including `n8n-community-node-package`, `sap`, `cap`, and `odata`, add `engines.node` aligned with research `>=22.16 <25`, add package-local dev dependencies for `@n8n/node-cli`, `n8n-workflow`, and `typescript` with the researched versions, and add the `n8n.nodes` and `n8n.credentials` manifest paths under `dist/`. Do not add credential auth modes, metadata discovery, Query, Read, Create, Update, Delete, Action, Function, or response cleanup behavior in this plan per D-17 through D-22.</action>
  <verify>
    <automated>node -e "const p=require('./cap-n8n-node/package.json'); if(!/^(@[^/]+\\/)?n8n-nodes-/.test(p.name)) process.exit(1); if(!p.keywords.includes('n8n-community-node-package')) process.exit(1); if(!p.n8n.nodes[0].includes('SapCap.node.js')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Package name follows n8n community-node naming conventions.
    - Package metadata includes n8n manifest paths for node and credentials.
    - Package scripts use repo-local n8n-node tooling.
  </acceptance_criteria>
  <done>`cap-n8n-node/package.json` can be inspected by n8n tooling as an SAP CAP community-node package.</done>
</task>

<task type="auto">
  <name>Task 2: Add loadable SAP CAP node and credential source files</name>
  <read_first>
    - .planning/phases/01-package-foundations-and-tooling/01-PATTERNS.md
    - mockups/n8n-node-mockup.html
  </read_first>
  <files>cap-n8n-node/nodes/SapCap/SapCap.node.ts, cap-n8n-node/credentials/SapCapApi.credentials.ts, cap-n8n-node/index.js, cap-n8n-node/tsconfig.json</files>
  <action>Create `SapCap.node.ts` and `SapCapApi.credentials.ts` using n8n TypeScript conventions from research. The node class should expose package-load metadata such as display name `SAP CAP`, name `sapCap`, group, version, description, defaults, inputs, outputs, and a credential reference to `sapCapApi`; it may expose a disabled or validation-oriented operation description only if required by n8n-node build/loadability. The credential class should expose `SAP CAP API`, `name: "sapCapApi"`, and non-secret structural fields needed for loadability such as base URL labels, without real credentials. Keep `index.js` either as a CommonJS metadata bridge or harmless package entry consistent with `main`; do not make it the source of n8n operations.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); for (const f of ['cap-n8n-node/nodes/SapCap/SapCap.node.ts','cap-n8n-node/credentials/SapCapApi.credentials.ts','cap-n8n-node/tsconfig.json']) if(!fs.existsSync(f)) process.exit(1); const node=fs.readFileSync('cap-n8n-node/nodes/SapCap/SapCap.node.ts','utf8'); if(!node.includes('SAP CAP')||!node.includes('sapCapApi')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Node and credential source files exist at the paths referenced by package metadata after build.
    - Files contain no API keys, private keys, personal production URLs, or committed credentials.
    - Files do not implement the deferred n8n CAP operation requirements reserved for Phases 6 and 7.
  </acceptance_criteria>
  <done>The n8n node package has concrete loadability files for NODE-01 without overreaching into later operation scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n runtime -> community-node package | n8n discovers node and credential classes from package metadata and compiled files. |
| package source -> package build | TypeScript source is compiled into dist paths referenced by the n8n manifest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | cap-n8n-node/package.json | mitigate | Use official n8n naming, keyword, scripts, and manifest path conventions from research. |
| T-01-04 | Information Disclosure | credential source file | mitigate | Only define structural credential fields and never commit real credential values. |
| T-01-05 | Elevation of Privilege | n8n node operation surface | accept | Phase 1 does not perform CAP HTTP operations; operation authorization is planned in later n8n phases. |
| T-01-SC | Tampering | npm package installs | mitigate | Only approved n8n packages from the research audit are declared here; lockfile install happens in Plan 04. |
</threat_model>

<verification>
Run task-level metadata and file checks. Plan 04 must later run `npm run build --workspace cap-n8n-node` after dependencies are installed and lockfiles are refreshed.
</verification>

<success_criteria>
NODE-01 is satisfied when `cap-n8n-node` has n8n community-node metadata, declared n8n manifest paths, and source files that n8n tooling can build and inspect.
</success_criteria>

<output>
Create `.planning/phases/01-package-foundations-and-tooling/01-02-SUMMARY.md` when done.
</output>
