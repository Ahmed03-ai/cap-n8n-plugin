---
phase: 01-package-foundations-and-tooling
plan: 04
subsystem: smoke-verification
tags: [npm-workspaces, vitest, n8n-node, smoke-tests, lockfiles]

requires: [01-01, 01-02, 01-03]
provides:
  - Root smoke command for package-boundary verification
  - Vitest package-boundary smoke test
  - Canonical root npm workspace lockfile
  - Non-failing workspace package test scripts
affects: [phase-01, FOUND-03, FOUND-04]

tech-stack:
  added:
    - vitest@4.1.7
    - eslint@9.29.0
    - "@eslint/js@9.29.0"
  patterns:
    - Build n8n package before smoke assertions import manifest modules
    - Root package-lock.json is canonical for the workspace

key-files:
  created:
    - test/smoke/package-boundaries.test.js
    - .planning/phases/01-package-foundations-and-tooling/01-04-SUMMARY.md
  modified:
    - package.json
    - package-lock.json
    - cap-n8n-plugin/package.json
    - cap-n8n-node/package.json
    - cap-n8n-node/tsconfig.json
    - cap-n8n-node/nodes/SapCap/SapCap.node.ts
    - .gitignore
  removed:
    - demo-app/package-lock.json

key-decisions:
  - "Approved and added vitest@4.1.7 after the required human verification checkpoint."
  - "Removed demo-app/package-lock.json because the root workspace package-lock.json is the canonical install state."
  - "Pinned eslint@9.29.0 and @eslint/js@9.29.0 to satisfy @n8n/node-cli's exact ESLint peer dependency without using --force or --legacy-peer-deps."
  - "Kept cap-n8n-node/dist ignored because it is generated by npm run smoke and package build commands."

requirements-completed: [FOUND-03, FOUND-04]

duration: 35min
completed: 2026-05-31
---

# Phase 01 Plan 04: Smoke Verification Summary

**Package-boundary smoke verification is now installed, repeatable, and backed by the canonical root workspace lockfile.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-05-31T12:19:00Z
- **Completed:** 2026-05-31T12:54:00Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments

- Added root `smoke` and `test` scripts. `npm run smoke` builds the n8n package first, then runs Vitest smoke checks.
- Added `test/smoke/package-boundaries.test.js`, which loads `cap-n8n-plugin` by package name and imports the compiled n8n node and credential modules referenced by `cap-n8n-node/package.json`.
- Replaced failing workspace package test scripts with commands that verify the CAP package export and n8n package build.
- Refreshed `package-lock.json` with CAP tooling, n8n node tooling, Vitest, and the final workspace metadata.
- Removed the stale nested `demo-app/package-lock.json` and recorded root `package-lock.json` as the workspace source of truth.
- Updated the n8n node TypeScript config and connection-type import so the current `n8n-node build` toolchain passes.

## Task Commits

- `533458f feat(01): add package smoke verification`

## Files Created/Modified

- `package.json` - Added root smoke/test scripts and approved dev dependencies.
- `package-lock.json` - Refreshed canonical workspace lockfile.
- `test/smoke/package-boundaries.test.js` - Integration-style package-boundary smoke test.
- `cap-n8n-plugin/package.json` - Replaced placeholder failing test script.
- `cap-n8n-node/package.json` - Replaced package test script with build verification.
- `cap-n8n-node/tsconfig.json` - Updated to Node16 module/moduleResolution for TypeScript 6 compatibility.
- `cap-n8n-node/nodes/SapCap/SapCap.node.ts` - Switched to the runtime `NodeConnectionTypes` export used by n8n workflow types.
- `.gitignore` - Ignored generated `cap-n8n-node/dist/`.
- `demo-app/package-lock.json` - Removed stale nested lockfile.

## Decisions Made

- Human approval for `vitest@4.1.7` was received before adding it to manifests or lockfiles.
- Added direct ESLint peer pins because `@n8n/node-cli@0.32.1` requires `eslint@9.29.0`, while the unconstrained install attempted an incompatible `@eslint/js` 10.x resolution.
- Did not commit generated `cap-n8n-node/dist/`; the smoke command proves it can be rebuilt and loaded.

## Deviations from Plan

- Added `eslint@9.29.0` and `@eslint/js@9.29.0` as root dev dependencies to resolve the n8n CLI peer dependency tree cleanly.
- Added `.gitignore` to keep generated n8n build output out of source control.
- The exact chained verification command in the plan could not run in this PowerShell session because `&&` is unsupported here, so the same commands were run one by one.

## Issues Encountered

- Initial `npm install` failed with an ESLint peer-resolution conflict. Pinning the n8n CLI's expected ESLint peer set resolved it without force flags.
- Initial `npm run smoke` found two n8n package build issues: deprecated TypeScript module resolution and `NodeConnectionType` being a type-only export. Both were fixed and reverified.
- `npm install` reports 18 transitive vulnerabilities from the installed dependency tree. No audit remediation was applied in this phase because it would likely require dependency upgrades outside the package-foundation scope.
- `n8n-node build` emits a Node deprecation warning for its internal child-process invocation. The build still succeeds.

## Verification

- `node --version` - passed: `v24.16.0`.
- `npm --version` - passed: `11.13.0`.
- `npm install` - passed; workspace lock is up to date.
- `npm run build --workspaces --if-present` - passed.
- `npm run smoke` - passed; 1 Vitest file and 2 tests passed.
- `npm test --workspaces --if-present` - passed.
- `npm test` - passed through the root smoke command.
- `npm pack --workspace cap-n8n-plugin --dry-run` - passed; tarball contains only package allowlist files.
- `npm pack --workspace n8n-nodes-sap-cap --dry-run` - passed; tarball contains generated dist node/credential modules, `index.js`, and package metadata.
- `docker compose config` - passed and renders `n8nio/n8n:2.22.5`.
- `git diff --check` - passed with only CRLF conversion warnings.

## Known Stubs

- The n8n SAP CAP node remains loadability-only. Query, Read, credentials auth modes, metadata discovery, mutations, actions/functions, and response cleanup remain reserved for later phases.

## Threat Flags

- Vitest was manually approved before installation.
- Package pack dry-runs did not include `.env`, `.n8n-data`, local workflow data, or generated tarballs.

## Self-Check: PASSED

- Root smoke command proves meaningful CAP plugin exports and manifest-referenced compiled n8n node and credential modules load after build.
- Workspace package test scripts no longer intentionally fail.
- Root `package-lock.json` reflects the final workspace manifests and dev dependencies.
- Nested demo lockfile drift was removed deliberately.

## Next Phase Readiness

Phase 2 can now build on real package boundaries, repo-local tooling, and a passing package-load smoke gate. Later n8n-specific phases can extend the `SapCap` node and `SapCapApi` credentials from the current loadability skeleton.

---
*Phase: 01-package-foundations-and-tooling*
*Completed: 2026-05-31*
