---
phase: 01-package-foundations-and-tooling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - demo-app/package.json
  - docker-compose.yml
autonomous: true
requirements:
  - FOUND-03
  - FOUND-05
must_haves:
  truths:
    - "FOUND-03: Developers can run repo-local CAP, n8n, and build commands through npm scripts without undocumented global installs."
    - "FOUND-05: Local n8n infrastructure uses a pinned image tag instead of n8nio/n8n:latest."
    - "Workspace package test scripts are no longer intentional failure commands after Plan 04 completes."
  artifacts:
    - path: "package.json"
      provides: "Root workspace scripts and local CAP tooling dependency declaration"
      contains: "cap:serve"
    - path: "demo-app/package.json"
      provides: "Demo CAP start script using local CAP CLI path"
      contains: "cds-serve"
    - path: "docker-compose.yml"
      provides: "Pinned local n8n Compose service"
      contains: "n8nio/n8n:2.22"
  key_links:
    - from: "package.json"
      to: "demo-app/package.json"
      via: "npm workspace script"
      pattern: "workspace demo-app"
    - from: "package.json"
      to: "docker-compose.yml"
      via: "n8n:up/import/export scripts"
      pattern: "docker compose"
---

<objective>
Localize repo tooling and pin the Docker-based n8n development runtime.

Purpose: This satisfies FOUND-03 and FOUND-05 while implementing D-11 and D-13. It also prepares D-12 for Plan 04 without adding the Vitest dependency before the required human verification gate.
Output: Root scripts for local CAP/build/n8n workflows, demo start compatibility, and pinned n8n Compose image.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-package-foundations-and-tooling/01-CONTEXT.md
@.planning/phases/01-package-foundations-and-tooling/01-RESEARCH.md
@.planning/phases/01-package-foundations-and-tooling/01-PATTERNS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@package.json
@demo-app/package.json
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-local CAP and workspace scripts</name>
  <read_first>
    - package.json
    - demo-app/package.json
    - .planning/phases/01-package-foundations-and-tooling/01-RESEARCH.md
  </read_first>
  <files>package.json, demo-app/package.json</files>
  <action>Implement D-11 by adding root scripts for `build`, `cap:serve`, `cap:compile`, `n8n:up`, `n8n:import`, and `n8n:export` using npm workspaces and Docker Compose. Add root `devDependencies.@sap/cds-dk` with the researched `9.9.1` version so CAP CLI commands are repo-local after Plan 04 refreshes the lockfile. Keep the demo app start command compatible with local CAP CLI resolution; prefer `cds-serve` only if npm scripts resolve it from local dependencies after install, otherwise use `cds serve` through the local CAP CLI. Do not add Vitest in this plan because D-15 smoke testing and the Vitest install are gated in Plan 04.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); for (const s of ['build','cap:serve','cap:compile','n8n:up','n8n:import','n8n:export']) if(!p.scripts[s]) process.exit(1); if(!p.devDependencies['@sap/cds-dk']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Root scripts are present for CAP serve/compile, workspace build, and n8n Compose workflows.
    - CAP CLI is declared as a repo-local dev dependency.
    - No test runner dependency is added before the Plan 04 checkpoint.
  </acceptance_criteria>
  <done>Developers have declared repo-local scripts for CAP and workspace build commands instead of hidden global assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Pin local n8n Docker infrastructure</name>
  <read_first>
    - docker-compose.yml
    - .planning/phases/01-package-foundations-and-tooling/01-RESEARCH.md
    - .planning/research/SUMMARY.md
  </read_first>
  <files>docker-compose.yml, package.json</files>
  <action>Implement D-13 by replacing `n8nio/n8n:latest` with a pinned `n8nio/n8n:2.22.5` tag or a digest-backed equivalent if the executor verifies a digest during implementation. Preserve the current ports, volumes, environment variables, and `test-workflows` mount. Keep package-load smoke independent from a running Docker service; `n8n:up`, `n8n:import`, and `n8n:export` should remain explicit local infrastructure commands.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$y = Get-Content -Raw docker-compose.yml; if ($y -match 'n8nio/n8n:latest') { exit 1 }; if ($y -notmatch 'n8nio/n8n:(2\\.22\\.5|sha256:)') { exit 1 }; docker compose config *> $null"</automated>
  </verify>
  <acceptance_criteria>
    - Compose file no longer uses `n8nio/n8n:latest`.
    - Existing n8n ports, volumes, and environment shape are preserved.
    - Docker Compose config validates.
  </acceptance_criteria>
  <done>Local n8n infrastructure is pinned enough for repeatable development behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell -> npm scripts | Developers invoke repo-local scripts that execute package manager and Docker commands. |
| Docker Compose -> local n8n runtime | Compose pulls and runs the pinned n8n image used by local workflow fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | docker-compose.yml image tag | mitigate | Pin n8n to `2.22.5` or a verified digest instead of `latest`. |
| T-01-07 | Tampering | root npm scripts | mitigate | Use explicit npm workspace scripts and local dev dependency declarations instead of hidden global commands. |
| T-01-08 | Denial of Service | n8n import/export scripts | accept | Import/export still require a running Docker service; package-load smoke remains independent in Plan 04. |
| T-01-SC | Tampering | npm package installs | mitigate | `@sap/cds-dk` is approved in the package legitimacy audit; install and lockfile refresh occur in Plan 04. |
</threat_model>

<verification>
Run the task-level checks plus `node --version`, `npm --version`, and `docker compose config`. Full install/build verification is owned by Plan 04.
</verification>

<success_criteria>
FOUND-03 and FOUND-05 are satisfied when repo-local CAP/build/n8n scripts exist, `@sap/cds-dk` is declared for local CLI use, and the n8n Compose image is pinned.
</success_criteria>

<output>
Create `.planning/phases/01-package-foundations-and-tooling/01-03-SUMMARY.md` when done.
</output>
