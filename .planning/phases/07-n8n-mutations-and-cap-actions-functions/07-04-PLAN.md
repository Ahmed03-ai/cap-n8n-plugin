---
phase: 07-n8n-mutations-and-cap-actions-functions
plan: 04
type: execute
wave: 4
depends_on: [07-02, 07-03]
files_modified:
  - README.md
  - docs/manual-visual-showcase.md
  - mockups/n8n-node-mockup.html
autonomous: true
requirements: [NODE-07, NODE-08, NODE-09, NODE-12, NODE-13, VERIFY-04]
must_haves:
  truths:
    - "README documents Phase 7 Create, Update, Delete, Action/Function, composite keys, JSON Body, JSON Parameters, and verification status."
    - "Manual visual showcase distinguishes real installed n8n custom-node E2E from deterministic integration verification."
    - "Mockup visibly shows Create, Update, Delete, and combined Action/Function controls matching D-01 through D-17."
    - "Docs no longer describe Phase 7 mutation/action modes as absent from the implemented SAP CAP node."
  artifacts:
    - path: "README.md"
      provides: "Phase 7 usage and verification documentation"
    - path: "docs/manual-visual-showcase.md"
      provides: "Presenter runbook for implemented node modes and honest verification boundaries"
    - path: "mockups/n8n-node-mockup.html"
      provides: "Visible n8n node editor showcase for mutation and Action/Function modes"
  key_links:
    - from: "README.md"
      to: "cap-n8n-node/nodes/SapCap/SapCap.node.ts"
      via: "documented operation names and parameter labels"
      pattern: "Create|Update|Delete|Action/Function"
    - from: "docs/manual-visual-showcase.md"
      to: "test/integration/n8n-node-read-operations.test.js"
      via: "verification status references deterministic integration coverage"
      pattern: "npm test|VERIFY-04"
---

<objective>
Update Phase 7 documentation and visual showcase so the implemented SAP CAP n8n node surface is accurately described and demonstrable.

Purpose: The node implementation is not complete for users or reviewers while README/manual/mockup artifacts still say Create, Update, Delete, and actions/functions are unavailable.
Output: Updated README usage docs, manual visual showcase guide, mockup controls, and source gates for stale read-only claims.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-n8n-mutations-and-cap-actions-functions/07-CONTEXT.md
@.planning/phases/07-n8n-mutations-and-cap-actions-functions/07-RESEARCH.md
@.planning/phases/07-n8n-mutations-and-cap-actions-functions/07-PATTERNS.md
@.planning/phases/07-n8n-mutations-and-cap-actions-functions/07-02-SUMMARY.md
@.planning/phases/07-n8n-mutations-and-cap-actions-functions/07-03-SUMMARY.md
@README.md
@docs/manual-visual-showcase.md
@mockups/n8n-node-mockup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Phase 7 SAP CAP node usage in README</name>
  <read_first>
    - README.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-CONTEXT.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-RESEARCH.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-PATTERNS.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-02-SUMMARY.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-03-SUMMARY.md
  </read_first>
  <files>README.md</files>
  <action>Update `README.md` sections that describe `cap-n8n-node/` and manual n8n node validation. Replace read-side-only wording with Phase 7 coverage for Query, Read, Create, Update, Delete, Action/Function, composite keys, response cleanup, and sanitized errors. Add concrete usage notes for Create Body JSON per D-01 through D-04, Update key plus Body JSON per D-01 through D-08, Delete key with no body and no confirmation checkbox per D-09 through D-12, Action/Function metadata/manual operation selection plus Parameters JSON per D-13 through D-17, and composite-key metadata/manual fallback per D-05 through D-08. Include verification commands `npm run build --workspace n8n-nodes-sap-cap`, focused Phase 7 integration suites, and `npm test`. Keep the existing caveat that the default Docker n8n service does not automatically install or mount the local community node.</action>
  <verify>
    <automated>rg -n "Create|Update|Delete|Action/Function|JSON Body|JSON Parameters|composite key|npm test|VERIFY-04" README.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: README names Create, Update, Delete, Action/Function, composite keys, JSON Body, JSON Parameters, and Phase 7 verification commands.
    - Source assertion: README no longer says Create, Update, Delete, or CAP actions/functions are unavailable in the implemented node surface.
    - Security assertion: README uses example-only values and does not include API keys, private keys, bearer tokens, Basic auth header values, OAuth client secrets, or production credentials.
    - Boundary assertion: README still states that installed real n8n custom-node E2E requires a separate install/mount path and is not implied by default Docker Compose.
  </acceptance_criteria>
  <done>README gives a CAP/n8n developer enough concrete Phase 7 usage and verification detail without overstating Phase 8 E2E readiness.</done>
</task>

<task type="auto">
  <name>Task 2: Update manual visual showcase for implemented modes and verification boundary</name>
  <read_first>
    - docs/manual-visual-showcase.md
    - README.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-CONTEXT.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-PATTERNS.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-02-SUMMARY.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-03-SUMMARY.md
  </read_first>
  <files>docs/manual-visual-showcase.md</files>
  <action>Update `docs/manual-visual-showcase.md` so the current implemented n8n node slice includes Create, Update, Delete, composite-key handling, and Action/Function mode. Move those modes out of the not-finished list while keeping polling triggers and automatic default-Docker custom-node installation outside the implemented showcase. Add presenter steps for what to open, what operation controls should be visible, what example Body/Parameters JSON looks like, how Delete success/not-found should be described, and which deterministic commands prove VERIFY-04. State plainly that real installed custom-node E2E in live n8n remains Phase 8 release-readiness evidence, while Phase 7 has deterministic built-node integration verification. This implements documentation coverage for D-01 through D-17 and VERIFY-04.</action>
  <verify>
    <automated>rg -n "Create|Update|Delete|Action/Function|JSON Body|JSON Parameters|composite key|deterministic integration|real n8n custom-node E2E|Phase 8|VERIFY-04" docs/manual-visual-showcase.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: manual showcase lists Create, Update, Delete, Action/Function, composite keys, Body JSON, and Parameters JSON under implemented/demoable node behavior.
    - Boundary assertion: manual showcase says real installed n8n custom-node E2E remains Phase 8 evidence and does not claim the default Docker n8n container includes the local node.
    - Verification assertion: manual showcase names `npm test` and focused integration verification as deterministic Phase 7 evidence.
    - Security assertion: examples are fake and do not contain committed secrets or real production metadata.
  </acceptance_criteria>
  <done>Manual showcase is honest about implemented Phase 7 behavior and the remaining live-n8n E2E boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh n8n node mockup and stale-doc source gates</name>
  <read_first>
    - mockups/n8n-node-mockup.html
    - README.md
    - docs/manual-visual-showcase.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-CONTEXT.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-PATTERNS.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-02-SUMMARY.md
    - .planning/phases/07-n8n-mutations-and-cap-actions-functions/07-03-SUMMARY.md
  </read_first>
  <files>mockups/n8n-node-mockup.html, README.md, docs/manual-visual-showcase.md</files>
  <action>Update `mockups/n8n-node-mockup.html` so the page subtitle and mode grids show the current Phase 7 surface: Query, Read, Create, Update, Delete, and Action/Function. Make Create/Update panels show one explicit Body JSON field per D-01 through D-04, show metadata-derived key controls plus manual Key Predicate fallback for Read/Update/Delete per D-05 through D-08, show Delete without any confirmation checkbox and with no body per D-09 through D-12, and add a visible Action/Function panel with metadata/manual operation selection plus Parameters JSON per D-13 through D-17. Run a non-planning-doc source gate with `rg` over `README.md`, `docs`, and `mockups` to ensure no stale read-only or unavailable mutation/action claims remain outside intentionally scoped Phase 8 caveats.</action>
  <verify>
    <automated>rg -n "Action/Function|Body \\(JSON\\)|Parameters \\(JSON\\)|Key Predicate|Create|Update|Delete" mockups/n8n-node-mockup.html; rg -n "Create, Update, Delete, CAP actions/functions.*not implemented|mutation/action modes are still|future concepts|current built SAP CAP node exposes only Query and Read" README.md docs mockups -g "*.md" -g "*.html" | rg -v ".planning" | rg -v "Phase 8|polling trigger" ; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - Visual source assertion: mockup subtitle and operation grids present Create, Update, Delete, and Action/Function as current Phase 7 node controls.
    - Visual source assertion: mockup does not show generated entity property editors, generated action/function parameter fields, a Delete confirmation checkbox, or raw response toggles.
    - Source-gate assertion: README/docs/mockups contain no stale non-Phase-8 claims that mutation/action modes are unavailable or only concepts.
    - Security assertion: mockup examples are fake and contain no secrets, tokens, auth headers, or production credentials.
  </acceptance_criteria>
  <done>Visual showcase matches Phase 7 node behavior, and stale read-only claims are removed from non-planning docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation summaries to docs | Recently implemented behavior becomes user-facing claims. |
| docs/mockup examples to repository | Example URLs, bodies, parameters, and credentials become committed artifacts. |
| visual Delete guidance to user behavior | Mockup and docs shape how users understand destructive Delete operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Information Disclosure | README, manual showcase, mockup | mitigate | Use fake example values only and source-gate docs for API keys, private keys, bearer tokens, Basic auth headers, OAuth client secrets, request bodies with secret-looking values, and production credentials. |
| T-07-04-02 | Tampering | documented URLs and key examples | mitigate | Document normalized service paths, explicit keys, and composite-key/manual fallback behavior; avoid examples that include URL boundary characters or unsafe path concatenation. |
| T-07-04-03 | Information Disclosure | Body and Parameters examples | mitigate | Keep examples small and business-generic; do not include real request bodies, customer data, tokens, or secret values. |
| T-07-04-04 | Tampering | destructive Delete documentation | mitigate | State Delete requires explicit key, sends no request body, has no extra confirmation checkbox per D-10, returns confirmation per D-09, and reports not-found by default per D-11. |
| T-07-04-05 | Repudiation | verification claims | mitigate | Distinguish deterministic Phase 7 integration verification from Phase 8 real installed n8n custom-node E2E, and name exact commands. |
| T-07-04-SC | Tampering | npm installs | mitigate | No package installs are planned for docs/showcase updates. |
</threat_model>

<verification>
Run the README/manual/mockup `rg` checks in each task, then run `npm test` if Plans 07-02 and 07-03 have not already recorded a passing root test in their summaries.
</verification>

<success_criteria>
Phase 7 docs and visual showcase accurately describe implemented Create, Update, Delete, composite-key, and Action/Function behavior, include verification status, and avoid stale read-only claims or secret-bearing examples.
</success_criteria>

<output>
Create `.planning/phases/07-n8n-mutations-and-cap-actions-functions/07-04-SUMMARY.md` when done.
</output>
