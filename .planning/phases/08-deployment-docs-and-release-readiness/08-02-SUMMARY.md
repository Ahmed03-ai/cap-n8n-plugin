---
phase: 08-deployment-docs-and-release-readiness
plan: 02
subsystem: docs-infra
tags: [n8n, docker-compose, custom-node, manual-uat, verify-07]
requires:
  - phase: 07-n8n-mutations-and-cap-actions-functions
    provides: Phase 7 SAP CAP node surface for Query, Read, Create, Update, Delete, composite keys, and Action/Function.
provides:
  - Isolated local n8n review profile that loads the local SAP CAP custom node from a repo-staged package install.
  - Helper script for building, packing, installing, and validating the local n8n node package shape.
  - Manual browser runbook for VERIFY-07 installed custom-node E2E coverage.
affects: [phase-08-release-readiness, docs, local-n8n-review]
tech-stack:
  added: [docker-compose profile, node helper script]
  patterns: [local npm pack staging, ignored review profile, manual UAT evidence checklist]
key-files:
  created:
    - docker-compose.n8n-node.yml
    - scripts/prepare-n8n-custom-node.js
    - docs/local-n8n-custom-node-e2e.md
  modified:
    - .gitignore
key-decisions:
  - "Use a separate docker-compose.n8n-node.yml review profile instead of changing default docker-compose.yml."
  - "Stage n8n-nodes-sap-cap through a local npm pack tarball under .n8n-review-data/custom."
  - "Keep VERIFY-07 proof as manual browser UAT with explicit evidence state manual UAT required."
patterns-established:
  - "Custom-node review setup uses .n8n-review-data/custom/node_modules/n8n-nodes-sap-cap as the installed package path."
  - "Manual browser evidence captures visible node loading, credential Test Connection, metadata options, CRUD, Action/Function, and cleanup."
requirements-completed: [DOCS-03, DOCS-07, VERIFY-07]
duration: 7min
completed: 2026-06-03
---

# Phase 08 Plan 02: Local n8n Custom-Node E2E Harness Summary

**Isolated n8n review profile with local SAP CAP node staging and a VERIFY-07 browser checklist**

## Performance

- **Duration:** 7 min
- **Started:** 2026-06-03T22:18:21Z
- **Completed:** 2026-06-03T22:25:23Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `docker-compose.n8n-node.yml` for an isolated n8n 2.22.5 review profile using `.n8n-review-data/` and `N8N_CUSTOM_EXTENSIONS`.
- Added `scripts/prepare-n8n-custom-node.js` to build, locally pack, install, and validate `n8n-nodes-sap-cap` without Docker, `.env`, registry package selection, or credentials.
- Added `docs/local-n8n-custom-node-e2e.md` with the manual browser proof path for node picker visibility, SAP CAP API credentials, Test Connection, metadata options, Query, Read, Create, Update, Delete, Action/Function, and cleanup.

## Task Commits

Per the execution prompt, both task outputs and this summary are committed together in one atomic `08-02` plan commit. `STATE.md` and roadmap updates were intentionally not staged or modified because phase-start bookkeeping is owned by the orchestrator.

## Files Created/Modified

- `docker-compose.n8n-node.yml` - Isolated local n8n review profile for loading custom extensions from `.n8n-review-data/custom`.
- `.gitignore` - Ignores `.n8n-review-data/` generated by the custom-node staging helper.
- `scripts/prepare-n8n-custom-node.js` - Builds, packs, installs, and validates the local n8n node package install shape.
- `docs/local-n8n-custom-node-e2e.md` - Manual VERIFY-07 browser runbook and evidence checklist.
- `.planning/phases/08-deployment-docs-and-release-readiness/08-02-SUMMARY.md` - This execution summary.

## Decisions Made

- Kept `docker-compose.yml` unchanged so the default local n8n setup remains plain n8n.
- Installed only the locally packed workspace tarball into the review profile using `npm install ./<tarball> --legacy-peer-deps`; the helper does not select a registry package.
- Left n8n owner/login values and SAP CAP API credentials as operator-entered browser state only.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed Windows npm process spawning**
- **Found during:** Task 1
- **Issue:** `node scripts/prepare-n8n-custom-node.js` failed when `spawnSync('npm.cmd')` returned `EINVAL` in this Windows runtime.
- **Fix:** Routed npm through `cmd.exe /d /s /c npm.cmd ...` on Windows and surfaced spawn errors in helper output.
- **Files modified:** `scripts/prepare-n8n-custom-node.js`
- **Verification:** `node scripts/prepare-n8n-custom-node.js` passed and installed `.n8n-review-data/custom/node_modules/n8n-nodes-sap-cap`.

**2. [Rule 1 - Verification Bug] Removed secret-regex false positive wording**
- **Found during:** Task 2
- **Issue:** The required inverted secret-like value check matched the documentation phrase "bearer tokens" even though it was guidance, not a secret.
- **Fix:** Reworded the warning to "token values" while preserving the no-secret instruction.
- **Files modified:** `docs/local-n8n-custom-node-e2e.md`
- **Verification:** The plan's inverted secret-like value check passed.

**Total deviations:** 2 auto-fixed (1 blocking, 1 verification bug).
**Impact on plan:** Both fixes were required for the planned verification path and did not expand scope.

## Issues Encountered

- The n8n node build emitted the existing `DEP0190` deprecation warning from `n8n-node build`; the build exited 0 and the warning is already documented as an accepted tooling warning in existing project docs.
- Real browser UAT was not performed; the plan output is the scripted setup and manual checklist, with evidence state intentionally left as `manual UAT required`.

## Verification

Passed:

- `node scripts/prepare-n8n-custom-node.js --check`
- `node scripts/prepare-n8n-custom-node.js`
- `node scripts/prepare-n8n-custom-node.js --check-install-shape`
- `docker compose -f docker-compose.n8n-node.yml config`
- `rg -n "N8N_CUSTOM_EXTENSIONS|n8n_cap_node_review|.n8n-review-data|node_modules/n8n-nodes-sap-cap|npm pack|npm install" docker-compose.n8n-node.yml .gitignore scripts/prepare-n8n-custom-node.js`
- `rg -n "VERIFY-07|SAP CAP|node picker|credentials screen|Docker Compose config does not prove custom-node loading|Test Connection|metadata-backed|Query|Read|Create|Update|Delete|Action/Function|Body \(JSON\)|Parameters \(JSON\)|host.docker.internal|manual UAT required" docs/local-n8n-custom-node-e2e.md`
- `node -e "const fs=require('fs'); const s=fs.readFileSync('docs/local-n8n-custom-node-e2e.md','utf8'); if(/ghp_|gho_|github_pat_|sk-[A-Za-z0-9]|BEGIN .*PRIVATE KEY|Authorization: Basic [A-Za-z0-9+/=]+|Bearer [A-Za-z0-9]|N8N_API_KEY=.*[A-Za-z0-9]/i.test(s)) process.exit(1)"`

Acceptance checks:

- `git diff -- docker-compose.yml` produced no diff.
- `.n8n-review-data/` appears as ignored generated data.
- Installed manifest files exist at `.n8n-review-data/custom/node_modules/n8n-nodes-sap-cap/dist/nodes/SapCap/SapCap.node.js` and `.n8n-review-data/custom/node_modules/n8n-nodes-sap-cap/dist/credentials/SapCapApi.credentials.js`.

## Known Stubs

None. The blank evidence fields in `docs/local-n8n-custom-node-e2e.md` are intentional manual UAT capture fields, not wired UI/data stubs.

## Threat Flags

None. The new Docker profile, package staging helper, and manual credential entry path are all covered by the plan threat model.

## User Setup Required

Manual browser UAT remains required for VERIFY-07. The reviewer must start CAP, run the staging helper, start `docker-compose.n8n-node.yml`, enter transient local n8n owner/login values if prompted, create SAP CAP API credentials in n8n, and complete the evidence checklist.

## Next Phase Readiness

Ready for the remaining Phase 8 plans. The local installed custom-node E2E path is now documented and scriptable, but the evidence state stays `manual UAT required` until a reviewer runs the browser checklist.

## Self-Check: PASSED

- Created files exist: `docker-compose.n8n-node.yml`, `scripts/prepare-n8n-custom-node.js`, `docs/local-n8n-custom-node-e2e.md`, and this summary.
- Modified file exists: `.gitignore`.
- Required verification commands passed.
- `STATE.md` remained unstaged and unmodified by this executor, per prompt.

---
*Phase: 08-deployment-docs-and-release-readiness*
*Completed: 2026-06-03*
