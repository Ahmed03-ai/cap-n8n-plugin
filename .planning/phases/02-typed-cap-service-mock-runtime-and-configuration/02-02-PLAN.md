---
phase: 02-typed-cap-service-mock-runtime-and-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - cap-n8n-plugin/lib/config.js
  - cap-n8n-plugin/lib/MockN8nWorkflowService.js
  - cap-n8n-plugin/lib/N8nWorkflowService.js
  - cap-n8n-plugin/cds-plugin.js
  - cap-n8n-plugin/index.js
  - demo-app/package.json
  - test/integration/n8n-mock-and-profiles.test.js
autonomous: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-03
  - CAPAPI-01
  - CAPAPI-02
  - CAPAPI-03
must_haves:
  truths:
    - "RUNTIME-01: CAP developers can use a deterministic local mock runtime without starting n8n."
    - "RUNTIME-02: CAP profiles/config switch between mock and webhook without application code changes."
    - "RUNTIME-03: non-development webhook mode fails clearly when baseUrl is missing unless kind: 'mock' is explicit."
    - "D-05: mock runtime is a deterministic CAP test double, not a fake n8n HTTP server."
    - "D-06: mock stores start records with future-compatible shape, but query/cancel expansion remains Phase 3."
    - "D-07: mock failures are explicit opt-in only."
    - "D-08: mock is selectable through config and development can fall back to mock when real settings are absent."
    - "D-09, D-10, D-11, D-12, D-21: use CAP profiles plus kind: 'mock' | 'webhook'; webhook mode requires baseUrl, auth is optional unless configured."
  artifacts:
    - path: "cap-n8n-plugin/lib/config.js"
      provides: "Runtime kind and credential/profile resolver"
      exports: ["resolveN8nConfig", "assertWebhookConfig"]
    - path: "cap-n8n-plugin/lib/MockN8nWorkflowService.js"
      provides: "Deterministic offline mock service"
      exports: ["MockN8nWorkflowService"]
    - path: "test/integration/n8n-mock-and-profiles.test.js"
      provides: "Integration tests for mock and config/profile behavior"
      contains: "kind: 'mock'"
  key_links:
    - from: "cap-n8n-plugin/cds-plugin.js"
      to: "cap-n8n-plugin/lib/MockN8nWorkflowService.js"
      via: "config-selected impl"
      pattern: "kind"
    - from: "cap-n8n-plugin/lib/MockN8nWorkflowService.js"
      to: "cap-n8n-plugin/lib/result.js"
      via: "shared result envelope"
      pattern: "createStartResult"
---

<objective>
Add mock runtime and CAP-native runtime selection for Phase 2.

Purpose: This satisfies RUNTIME-01 through RUNTIME-03 by letting developers choose `kind: 'mock' | 'webhook'`, default safely to mock in development when real webhook settings are absent, and fail clearly outside development when webhook configuration is incomplete.
Output: Config resolver, mock service implementation, bootstrap selection, demo config refresh, and mock/profile integration tests.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-CONTEXT.md
@.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-RESEARCH.md
@.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-PATTERNS.md
@cap-n8n-plugin/lib/N8nWorkflowService.js
@cap-n8n-plugin/lib/result.js
@cap-n8n-plugin/cds-plugin.js
@cap-n8n-plugin/index.js
@cap-n8n-plugin/package.json
@demo-app/package.json
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL | Switch between mock and real n8n profiles | This plan owns runtime selection, mock fallback, and startup/config validation. |
| REQ | RUNTIME-01 | `MockN8nWorkflowService` provides offline deterministic start behavior. |
| REQ | RUNTIME-02 | `resolveN8nConfig` and `cds-plugin.js` support config/profile switching. |
| REQ | RUNTIME-03 | `assertWebhookConfig` fails non-development webhook mode with missing `baseUrl`. |
| REQ | CAPAPI-01, CAPAPI-02, CAPAPI-03 | Mock service implements the same start contract and result envelope as Plan 01. |
| CONTEXT | D-05, D-06, D-07, D-08 | Mock is deterministic, start-record-only, explicit-failure-capable, and profile/config selected. |
| CONTEXT | D-09, D-10, D-11, D-12, D-21 | CAP profiles plus `kind: 'mock' | 'webhook'`; development fallback only; webhook requires baseUrl. |
| RESEARCH | CAP profiles and `cds.env` | Use CAP-native `cds.requires.n8n` and profile semantics instead of app-code branching. |
| PATTERNS | CAP config binding and package export map | Extend existing config/exports without breaking `cap-n8n-plugin/service`. |

Phase 3 query/cancel/persistence and Phase 8 deployment docs remain deferred.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add config/profile resolver tests</name>
  <read_first>
    - demo-app/package.json
    - cap-n8n-plugin/cds-plugin.js
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-CONTEXT.md
  </read_first>
  <files>test/integration/n8n-mock-and-profiles.test.js</files>
  <action>Create `test/integration/n8n-mock-and-profiles.test.js` with Vitest cases for `resolveN8nConfig`. Assert explicit `{ kind: 'mock' }` resolves mock. Assert development profile or non-production environment with no `baseUrl` resolves mock. Assert `{ kind: 'webhook', credentials: { baseUrl: 'http://localhost:5678' } }` resolves webhook. Assert production/non-development webhook mode without `baseUrl` throws a sanitized configuration error containing `baseUrl` and not containing any API key value. Include a case proving `apiKey` is optional when `baseUrl` is present.</action>
  <verify>
    <automated>npx vitest run test/integration/n8n-mock-and-profiles.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Tests mention `kind: 'mock'`.
    - Tests mention `kind: 'webhook'`.
    - Tests prove development fallback to mock when no `baseUrl` exists.
    - Tests prove non-development webhook mode without `baseUrl` throws.
    - Tests prove `apiKey` is optional when `baseUrl` exists.
  </acceptance_criteria>
  <done>The expected profile/config behavior is locked before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runtime configuration resolution</name>
  <read_first>
    - cap-n8n-plugin/lib/N8nWorkflowService.js
    - cap-n8n-plugin/cds-plugin.js
    - demo-app/package.json
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-RESEARCH.md
  </read_first>
  <files>cap-n8n-plugin/lib/config.js</files>
  <action>Create `cap-n8n-plugin/lib/config.js` as a CommonJS helper exporting `resolveN8nConfig(options = {}, env = process.env)` and `assertWebhookConfig(config)`. The resolver should read `options.kind`, `options.mode`, `options.credentials.baseUrl`, `options.baseUrl`, `options.credentials.apiKey`, `options.apiKey`, retry/timeout defaults for later Plan 03, and profile hints from `env.CDS_ENV` / `env.NODE_ENV`. Use runtime kind values `mock` and `webhook`. If kind is absent and no real `baseUrl` exists in development, resolve `kind: 'mock'`; if kind is absent and `baseUrl` exists, resolve `kind: 'webhook'`; if production/non-development resolves webhook without `baseUrl`, throw a sanitized error. Do not read or write files from this helper.</action>
  <verify>
    <automated>node -e "const {resolveN8nConfig}=require('./cap-n8n-plugin/lib/config'); if(resolveN8nConfig({kind:'mock'}).kind!=='mock') process.exit(1); if(resolveN8nConfig({credentials:{baseUrl:'http://x'}}).kind!=='webhook') process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `config.js` exports `resolveN8nConfig`.
    - `config.js` exports `assertWebhookConfig`.
    - Explicit `kind: 'mock'` resolves mock.
    - Explicit or inferred webhook mode requires `baseUrl` outside development fallback.
    - The helper does not log secrets or include API key values in thrown messages.
  </acceptance_criteria>
  <done>Runtime selection can be tested independently from CAP bootstrap timing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic mock workflow service</name>
  <read_first>
    - cap-n8n-plugin/lib/N8nWorkflowService.js
    - cap-n8n-plugin/lib/result.js
    - cap-n8n-plugin/lib/config.js
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-PATTERNS.md
  </read_first>
  <files>cap-n8n-plugin/lib/MockN8nWorkflowService.js, cap-n8n-plugin/index.js</files>
  <action>Create `cap-n8n-plugin/lib/MockN8nWorkflowService.js` as a `cds.Service` subclass with the same `start(workflowId, inputs = {}, options = {})` method and `this.on('start', ...)` compatibility as `N8nWorkflowService`. Store start records in memory on the service instance in an array such as `this.executions`. Generate deterministic mock IDs with a monotonic counter like `mock-exec-1`. Each record must include `executionId`, `workflowId`, `inputs`, `status: 'success'`, `startedAt`, `finishedAt`, optional `correlationId`, and optional `businessKey`. Support explicit opt-in failures through config, for example `options.mock.failWorkflows` or `options.failWorkflows`, matching workflow IDs and throwing a sanitized mock error. Export `MockN8nWorkflowService` from `cap-n8n-plugin/index.js` if this is useful for tests; do not add query/cancel methods.</action>
  <verify>
    <automated>npx vitest run test/integration/n8n-mock-and-profiles.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `MockN8nWorkflowService` extends `cds.Service`.
    - Mock `start()` returns `mock: true` and an `executionId` like `mock-exec-1`.
    - Mock records include `workflowId`, `inputs`, `status`, timestamps, and optional correlation/business keys.
    - Mock failures happen only when explicitly configured.
    - No query/cancel methods are added in Phase 2.
  </acceptance_criteria>
  <done>Developers can run the plugin contract offline with deterministic mock behavior.</done>
</task>

<task type="auto">
  <name>Task 4: Wire mock/webhook selection into package bootstrap and demo config</name>
  <read_first>
    - cap-n8n-plugin/cds-plugin.js
    - cap-n8n-plugin/package.json
    - cap-n8n-plugin/lib/config.js
    - cap-n8n-plugin/lib/MockN8nWorkflowService.js
    - demo-app/package.json
  </read_first>
  <files>cap-n8n-plugin/cds-plugin.js, cap-n8n-plugin/package.json, demo-app/package.json</files>
  <action>Update `cds-plugin.js` so it preserves an explicit `cds.env.requires.n8n.impl` but fills a missing implementation based on resolved `kind`: mock uses `require.resolve('./lib/MockN8nWorkflowService.js')`, webhook uses `require.resolve('./lib/N8nWorkflowService.js')`. Keep `cap-n8n-plugin/service` as the webhook service export for the demo app. Add any package export subpaths needed for the mock service only if tests or CAP config require them, for example `"./mock-service": "./lib/MockN8nWorkflowService.js"`. Refresh `demo-app/package.json` to show a CAP-native `kind: "webhook"` for its local real n8n binding without removing `credentials.baseUrl`, `credentials.apiKey`, or server port.</action>
  <verify>
    <automated>npx vitest run test/integration/n8n-mock-and-profiles.test.js test/smoke/package-boundaries.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `cds-plugin.js` never overwrites an explicit `cds.env.requires.n8n.impl`.
    - Missing impl can resolve to mock when `kind: 'mock'`.
    - Missing impl can resolve to webhook when `kind: 'webhook'`.
    - `demo-app/package.json` retains `impl`, `baseUrl`, `apiKey`, and port `3000`.
    - Package-boundary smoke tests still pass.
  </acceptance_criteria>
  <done>CAP apps can select mock or webhook mode without changing business code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CAP profile/config -> runtime implementation | Config decides whether calls use mock memory or real webhook transport. |
| developer machine -> production runtime | Development fallback must not silently mask production missing credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | runtime kind selection | mitigate | Use explicit `kind: 'mock' | 'webhook'` and preserve explicit `impl` overrides; tests assert selection behavior. |
| T-02-05 | Information Disclosure | config errors | mitigate | Throw sanitized config errors that name missing keys but never include API key values or auth headers. |
| T-02-06 | Tampering | mock execution store | accept | Store mock executions in process memory only for deterministic development/tests; do not present it as durable execution tracking. |
</threat_model>

<verification>
Run `npx vitest run test/integration/n8n-mock-and-profiles.test.js test/smoke/package-boundaries.test.js`.
</verification>

<success_criteria>
RUNTIME-01, RUNTIME-02, and RUNTIME-03 are satisfied when mock mode works offline, webhook mode is selectable with `baseUrl`, development can fall back to mock, and non-development webhook config fails clearly when required settings are absent.
</success_criteria>

<output>
Create `.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-02-SUMMARY.md` when done.
</output>
