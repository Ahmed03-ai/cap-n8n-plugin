---
phase: 02-typed-cap-service-mock-runtime-and-configuration
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - package.json
  - test/integration/n8n-service-contract.test.js
  - test/integration/n8n-mock-and-profiles.test.js
  - test/integration/n8n-webhook-runtime.test.js
  - test/smoke/package-boundaries.test.js
  - demo-app/package.json
autonomous: true
requirements:
  - VERIFY-01
  - CAPAPI-01
  - CAPAPI-02
  - CAPAPI-03
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-03
  - RUNTIME-04
  - RUNTIME-05
must_haves:
  truths:
    - "VERIFY-01: developers can run integration tests for CAP programmatic API, authentication, errors, retry behavior, and mock runtime."
    - "All Phase 2 requirement IDs are covered by repeatable local verification without requiring live Docker n8n."
    - "D-01 through D-19 are reflected in integration tests, aggregate scripts, or explicit Phase 3/5/6/7 deferrals."
    - "D-20 and D-21: exact helper/test names may vary, but package-owned CommonJS CAP conventions and kind: 'mock' | 'webhook' preference are preserved."
  artifacts:
    - path: "package.json"
      provides: "Root integration test script"
      contains: "test:integration"
    - path: "test/integration/n8n-service-contract.test.js"
      provides: "Programmatic API integration coverage"
      contains: "start("
    - path: "test/integration/n8n-mock-and-profiles.test.js"
      provides: "Mock/profile integration coverage"
      contains: "kind"
    - path: "test/integration/n8n-webhook-runtime.test.js"
      provides: "Webhook reliability integration coverage"
      contains: "retry"
  key_links:
    - from: "package.json"
      to: "test/integration"
      via: "npm script"
      pattern: "vitest run test/integration"
---

<objective>
Wire Phase 2 integration verification into repeatable project commands and close coverage gaps.

Purpose: This satisfies VERIFY-01 and confirms all Phase 2 CAP API/runtime requirements are covered by deterministic integration tests, while preserving Phase 1 package-boundary smoke coverage.
Output: Root test scripts, final integration-test adjustments, aggregate verification command, and coverage clean-up.
</objective>

<execution_context>
@C:/Users/leonk/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/leonk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-CONTEXT.md
@.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-RESEARCH.md
@.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-PATTERNS.md
@package.json
@test/smoke/package-boundaries.test.js
@demo-app/package.json
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL | Phase 2 runtime behavior is repeatably verifiable | This plan owns aggregate command wiring and final coverage checks. |
| REQ | VERIFY-01 | Adds/validates `npm run test:integration` and makes root test include Phase 2 integration coverage. |
| REQ | CAPAPI-01, CAPAPI-02, CAPAPI-03 | Ensures Plan 01 tests are in the aggregate command. |
| REQ | RUNTIME-01, RUNTIME-02, RUNTIME-03 | Ensures Plan 02 tests are in the aggregate command. |
| REQ | RUNTIME-04, RUNTIME-05 | Ensures Plan 03 tests are in the aggregate command. |
| CONTEXT | D-01 through D-04 | Covered by service-contract test file and aggregate script. |
| CONTEXT | D-05 through D-12 | Covered by mock/profile test file and aggregate script. |
| CONTEXT | D-13 through D-16 | Covered by webhook runtime test file and aggregate script. |
| CONTEXT | D-17 through D-19 | Covered by envelope/error assertions and explicit deferred-scope checks. |
| CONTEXT | D-20, D-21 | Plan keeps helper/test naming conventional and preserves `kind` preference. |
| RESEARCH | Integration-test matrix | This plan maps the matrix into runnable scripts. |

No Phase 2 source item is intentionally left unplanned.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add root integration test script</name>
  <read_first>
    - package.json
    - test/smoke/package-boundaries.test.js
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-RESEARCH.md
  </read_first>
  <files>package.json</files>
  <action>Add a root script `"test:integration": "vitest run test/integration"` to `package.json`. Update `"test"` to run both existing smoke coverage and integration coverage, for example `"npm run smoke && npm run test:integration"`. Preserve `"smoke": "npm run build --workspace n8n-nodes-sap-cap && vitest run test/smoke"` and existing n8n/CAP scripts. Do not require Docker n8n for `test:integration`.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['test:integration']) process.exit(1); if(!p.scripts.test.includes('test:integration')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `scripts.test:integration`.
    - `package.json` `scripts.test` invokes `test:integration`.
    - `scripts.smoke` remains present.
    - No script added by this task runs `docker compose up`.
  </acceptance_criteria>
  <done>Developers have a repeatable command for Phase 2 integration coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close integration coverage gaps across all Phase 2 tests</name>
  <read_first>
    - test/integration/n8n-service-contract.test.js
    - test/integration/n8n-mock-and-profiles.test.js
    - test/integration/n8n-webhook-runtime.test.js
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-CONTEXT.md
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-RESEARCH.md
  </read_first>
  <files>test/integration/n8n-service-contract.test.js, test/integration/n8n-mock-and-profiles.test.js, test/integration/n8n-webhook-runtime.test.js</files>
  <action>Review all Phase 2 integration tests and add missing assertions so the suite covers every Phase 2 requirement and decisions D-01 through D-19. Ensure tests explicitly cover package-level service loading, `start()` convenience API, `send('start')` compatibility, structured optional `executionId`, mock start records, explicit mock failures, development fallback, non-development missing `baseUrl` failure, optional API key behavior, transient retry, non-retryable error, timeout, sanitized error fields, and schema-friendly result/error envelopes. Keep tests deterministic and avoid live n8n.</action>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:integration` exits 0.
    - At least one integration test asserts `executionId` may be absent without failing success.
    - At least one integration test asserts mock records include `status`.
    - At least one integration test asserts a configured secret value is absent from error message/details.
    - No integration test requires Docker or a live n8n container.
  </acceptance_criteria>
  <done>VERIFY-01 is represented by deterministic integration tests rather than only smoke checks.</done>
</task>

<task type="auto">
  <name>Task 3: Verify demo binding and package-boundary compatibility</name>
  <read_first>
    - demo-app/package.json
    - demo-app/srv/admin-service.js
    - cap-n8n-plugin/package.json
    - cap-n8n-plugin/index.js
    - test/smoke/package-boundaries.test.js
  </read_first>
  <files>demo-app/package.json, test/smoke/package-boundaries.test.js</files>
  <action>Adjust only if needed so the demo app still uses `cap-n8n-plugin/service`, retains `cds.requires.n8n.credentials.baseUrl`, retains `apiKey: "{env.N8N_API_KEY}"`, and can keep its existing `n8n.send('start', ...)` business handler. Extend `test/smoke/package-boundaries.test.js` only if package exports changed, for example to assert a public mock service export. Avoid changing the demo business handler unless a compatibility bug requires it.</action>
  <verify>
    <automated>npm run smoke</automated>
  </verify>
  <acceptance_criteria>
    - `demo-app/package.json` still contains `"impl": "cap-n8n-plugin/service"`.
    - `demo-app/package.json` still contains `"baseUrl": "http://localhost:5678"`.
    - `demo-app/package.json` still contains `"apiKey": "{env.N8N_API_KEY}"`.
    - `npm run smoke` exits 0.
  </acceptance_criteria>
  <done>Phase 2 runtime changes do not regress Phase 1 package-boundary evidence or the demo binding.</done>
</task>

<task type="auto">
  <name>Task 4: Run final Phase 2 verification commands</name>
  <read_first>
    - package.json
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-CONTEXT.md
    - .planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-RESEARCH.md
  </read_first>
  <files>package.json</files>
  <action>Run the final verification command set and fix only narrow failures in Phase 2-owned files: `npm run test:integration`, `npm run smoke`, `npm run cap:compile`, and `npm test`. If `npm test` already includes smoke and integration, still run `npm test` as the final aggregate. Record any environment-only limitation in the plan summary, not in source code.</action>
  <verify>
    <automated>npm run test:integration</automated>
    <automated>npm run smoke</automated>
    <automated>npm run cap:compile</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:integration` exits 0.
    - `npm run smoke` exits 0.
    - `npm run cap:compile` exits 0.
    - `npm test` exits 0.
    - Any skipped or failed command is documented in `02-04-SUMMARY.md` with exact output and reason.
  </acceptance_criteria>
  <done>Phase 2 is ready for verification review with a repeatable local command set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI -> test suite | Local commands verify behavior without needing live n8n or secrets. |
| test fixtures -> repository | Test fixtures must not commit real credentials or production URLs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | integration tests | mitigate | Use fake local HTTP servers and dummy secret strings; assert secrets are not exposed in errors. |
| T-02-12 | Tampering | aggregate test scripts | mitigate | Preserve Phase 1 smoke script and add integration script explicitly instead of replacing coverage. |
| T-02-13 | Denial of Service | test runtime | mitigate | Tests must use short local server timeouts and no Docker n8n dependency. |
</threat_model>

<verification>
Run `npm run test:integration`, `npm run smoke`, `npm run cap:compile`, and `npm test`.
</verification>

<success_criteria>
VERIFY-01 is satisfied when the repository has deterministic integration tests for CAP programmatic API, authentication/header behavior, errors, retries, timeout, and mock runtime, and those tests run through a documented npm command.
</success_criteria>

<output>
Create `.planning/phases/02-typed-cap-service-mock-runtime-and-configuration/02-04-SUMMARY.md` when done.
</output>
