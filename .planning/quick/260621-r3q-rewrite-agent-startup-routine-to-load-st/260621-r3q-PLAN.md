---
status: complete
quick_id: 260621-r3q
description: Rewrite agent startup routine to load stock update discord msg test workflow
created: 2026-06-21
---

# Quick Task 260621-r3q Plan

Rewrite the existing agent startup routine so its default n8n import loads the checked-in workflow named `stock update discord msg test workflow`.

## Tasks

1. Pull current remote state
   - Verify the worktree state and pull with rebase.
   - Confirm whether the named workflow fixture exists after pull.

2. Update startup behavior
   - Change the default workflow import to target `stock update discord msg test workflow`.
   - Keep stable workflow ID normalization for n8n imports.
   - Fail clearly if the selected workflow cannot be found.
   - Avoid printing fixture secret values.

3. Update docs
   - Update README command descriptions and the agent startup section with the new default workflow behavior.

4. Verify
   - Run JavaScript syntax checks.
   - Run startup help and preflight checks.
   - Run the import path against local n8n when available.
