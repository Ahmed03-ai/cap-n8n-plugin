# Quick Task 260712-v5z: Install the supplied local book recommendation workflow

## Scope

Replace the default startup workflow fixture with the supplied local book recommendation workflow. Keep it local to this repository and do not push it.

## Plan

1. Remove exported instance metadata, local credential bindings, and the live Discord webhook URL from the supplied workflow.
2. Configure the fixture to use the existing `DISCORD_WEBHOOK_URL` environment variable and to import as active.
3. Write the sanitized workflow to `test-workflows/stock update discord msg test workflow.json` and validate its JSON and safety invariants.
