---
status: complete
quick_id: 260621-nt4
description: Build agentic startup routine for local CAP and n8n review
created: 2026-06-21
---

# Quick Task 260621-nt4 Plan

Build a repo-local startup helper that lets any reviewer start the CAP demo and the custom-node n8n review profile with environment checks and clear next steps.

## Tasks

1. Add a startup script
   - Files: `scripts/agent-startup-routine.js`, `package.json`
   - Action: create a Node.js helper that checks Node/npm/Docker/ports, prepares the local n8n custom node, starts n8n through available Compose tooling, imports the demo workflow fixture, starts CAP, polls health endpoints, and prints the browser handoff.
   - Verify: run the helper with `--check` and then run the real startup path as far as this machine allows.

2. Document the command
   - Files: `README.md`, `.gitignore`
   - Action: add a short agent startup routine section that points reviewers to the helper command, explains the workflow import, and explains the fallback behavior.
   - Verify: README command list includes the new script and does not overstate browser-level n8n validation.

3. Record completion
   - Files: `.planning/STATE.md`, `.planning/quick/260621-nt4-build-agentic-startup-routine-for-local-/260621-nt4-SUMMARY.md`
   - Action: capture what changed, verification evidence, and any remaining manual blocker.
   - Verify: quick-task table has a new row.
